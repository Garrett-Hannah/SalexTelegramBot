telegram:
  bot:
    token: ${BOT_TOKEN:}
    username: ${BOT_USERNAME:}

bot:
  database:
    jdbc-url: ${JDBC_URL:}
    username: ${DB_USER:}
    password: ${DB_PASS:}
    validation-timeout-seconds: ${DB_VALIDATION_TIMEOUT_SECONDS:2}
